# Telifli kaynak kitaplar: yalnız kural çıkarmak için yerelde kullanıldı
pdf/
*.pdf
*.epub

# Araştırma dökümleri ve ham kaynaklar (başka projelerin ve TDK'nın metinleri)
_kaynak/

# Kalibrasyon derlemi: haber metinleri ve üretilmiş metinler, yeniden üretilebilir
tests/kalibrasyon/derlem/
tests/kalibrasyon/derlem-site/

# İlk taslaklar: LLM görüşleri (_kaynak'a taşındı)
chatgpt.md
deepseek.md
gemini.md
grok.md
z-ai.md
New Text Document.txt

# Araç dizinleri
.vexp/
node_modules/
.env
.env.*

# TDK dizininden yerelde derlenen sözlük (TDK verisi, dağıtılmaz)
skills/turkish-writer/scripts/data/tdk-dizin.json

# Kullanıcının kendi ses örnekleri (telifli olabilir)
skills/turkish-writer/references/ornekler/yerel/

# Kör test derlemi: insan metinleri telifli
tests/kalibrasyon/derlem-beceri/

# OSCAR 2019'dan yerelde sayılan eş dizim verisi ve indirme önbelleği (dağıtılmaz)
skills/turkish-writer/scripts/data/esdizim.json
skills/turkish-writer/scripts/data/esdizim-onbellek/

# Fark derlemi: insan metinleri telifli
tests/kalibrasyon/derlem-fark/

# Yarışma (kör karşılaştırma): insan metinleri telifli
tests/kalibrasyon/yarisma/

# Nitelikli insan derlemi ve üretilmiş karşılıkları: insan metinleri telifli olabilir, yalnız istatistik
tests/kalibrasyon/derlem-nitelikli/

# Model kör testi: anahtar ve ham üretim önbelleği git dışı (set kör kalsın)
tests/kalibrasyon/kor-test/model-anahtar-*.json
tests/kalibrasyon/kor-test/model-ham-*.json

# İnsan mı beceri mi kör testi: anahtar ve hakem sonuçları git dışı (set kör kalsın).
# set-1.json da git dışı: insan parçaları CC BY-NC-ND kaynaklı, derlem "dağıtılmıyor" dayanağıyla alındı.
tests/kalibrasyon/kor-test/cevap-anahtari-*.json
tests/kalibrasyon/kor-test/set-*.json

# Kör test 2 kaynakları: seçim/brif dosyaları, Journo ham metinleri, döngü kayıtları (set kör kalsın)
tests/kalibrasyon/kor-test/set-2-kaynak/

# Bağımsız tekli hakem derlemi (tur 3): üretilmiş metin ve hakem önbelleği, yeniden üretilebilir
tests/kalibrasyon/derlem-tekli/

# Tanıma kontrolü (tur 4): 2025+ yayımlanmış insan metinleri, yalnız iç ölçüm
tests/kalibrasyon/derlem-yeni-insan/
